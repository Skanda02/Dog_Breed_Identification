# Base URL of the backend API.
# In production (frontend served by the backend) leave this unset so requests
# use the relative "/api" path. Set it only when the frontend and backend are
# hosted on different origins.
VITE_API_URL=http://localhost:5001/api
